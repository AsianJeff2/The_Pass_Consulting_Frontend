# Server-only values. Never prefix these with NEXT_PUBLIC_.
RESEND_API_KEY=
# Verify thepassconsulting.com in Resend. Other sender mailboxes are rejected.
CONTACT_FROM=The Pass <inquiries@thepassconsulting.com>
# Your production origin, without a path. Local development accepts localhost.
SITE_URL=http://localhost:3000
